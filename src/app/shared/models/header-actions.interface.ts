export interface HeaderAction {
  label: string;
  icon?: string;        // e.g. "add" or a font-awesome class
  primary?: boolean;     // toggles your “primary” style
  id: string;            // unique identifier
}
